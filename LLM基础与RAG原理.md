# LLM 基础与 RAG 原理

---

## 一、LLM 基础概念

### 1. 上下文（Context）

LLM 每次对话时能"看到"的全部文本范围，包括：

```
System Prompt → 历史对话 → 当前用户输入
└─────────────────────────────────────────┘
              这整块就是"上下文"
```

- **上下文窗口（Context Window）**：模型一次最多能处理的 token 数量上限
- 超出窗口的内容会被**截断**，模型就"忘记"了
- 通过对话历史管理（Chat Memory）控制上下文长度，避免超出限制

---

### 2. Token

Token 是模型处理文本的**最小单元**，不等于字符：

| 文本 | Token 数（近似） |
|------|----------------|
| `Hello` | 1 |
| `你好` | 2 |
| `诊断任务异常` | 4~6 |

- **计费单位**：API 按输入 + 输出 token 总量收费
- **影响速度**：token 越多，生成越慢
- 可通过配置 `maxTokens` 控制单次最大输出量

---

### 3. 温度（Temperature）

控制模型输出的**随机性 / 创造性**，取值通常 `0.0 ~ 2.0`：

```
temperature = 0.0  →  确定性最强，每次输出几乎相同（适合诊断/分析）
temperature = 0.7  →  平衡创造性与稳定性（通用对话）
temperature = 1.5+ →  高随机性，输出多样但可能不稳定（创作场景）
```

> AI 诊断场景建议使用**低温度**（0.1~0.3），保证诊断结论稳定可重复。

---

### 4. 幻觉（Hallucination）

模型"编造"看似合理但实际**不存在或错误**的内容：

```
用户问：任务 #12345 为什么失败？
模型答：任务 #12345 因为传感器 B 超温导致失败。（实际上是瞎编的）
```

**应对策略：**

- 使用向量检索知识库，用真实文档"接地"
- 查询真实系统日志和任务数据，而非靠模型"记忆"
- RAG（检索增强生成）是抑制幻觉的核心手段

---

### 5. System Prompt

对话开始前注入的**隐藏指令**，设定模型的角色、行为规则、输出格式：

```
你是一个WCS智能诊断专家，只能回答与仓储系统故障相关的问题，
回答必须基于提供的日志数据，不得猜测...
```

System Prompt 在整个对话中持续生效，是控制模型行为最直接的方式。

---

### 概念关系总结

```
┌─────────────────────────────────────────┐
│           上下文窗口 (Context Window)     │
│  ┌──────────────┐  ┌──────────────────┐ │
│  │ System Prompt│  │  对话历史 + 输入  │ │
│  │ (角色/规则)  │  │   (消耗 Tokens)  │ │
│  └──────────────┘  └──────────────────┘ │
└─────────────────────────────────────────┘
         ↓ 模型推理（受 Temperature 影响）
    输出结果（可能含幻觉，需 RAG 抑制）
```

---

## 二、RAG 与微调

### RAG（检索增强生成）

#### 核心思想

模型本身不"记住"所有知识，而是**用时去查，查到再答**。

```
用户提问
   ↓
① 将问题向量化（Embedding）
   ↓
② 去向量数据库里检索最相似的文档片段
   ↓
③ 把检索到的内容 + 原始问题 一起塞进 Prompt
   ↓
④ 模型基于"参考资料"生成回答
```

#### 类比理解

| 场景 | 类比 |
|------|------|
| 没有 RAG | 闭卷考试，全靠背过的知识 |
| 有 RAG | 开卷考试，可以翻参考书 |

#### RAG 解决了什么问题

| 问题 | RAG 的解决方式 |
|------|---------------|
| 知识时效性 | 模型训练截止后的新知识，RAG 随时可补充 |
| 私有数据 | 企业内部文档、操作手册，不需要训练模型 |
| 幻觉抑制 | 回答有据可查，减少模型编造 |
| 可解释性 | 可以告诉用户"答案来自第X页文档" |

---

### 微调（Fine-tuning）

#### 核心思想

**在预训练大模型的基础上，用特定领域数据继续训练**，让模型的"权重"本身发生变化，内化特定知识或风格。

```
通用大模型（GPT/GLM 等）
   ↓ 喂入领域数据（问答对、文档等）继续训练
领域专属模型（权重已更新）
```

#### 类比理解

| 场景 | 类比 |
|------|------|
| 预训练 | 培养一个博学的大学生 |
| 微调 | 让他再读 3 年医学研究生，成为医生 |
| RAG | 让大学生做题时可以查医学词典 |

#### 微调的代价

| 维度 | 代价 |
|------|------|
| 计算资源 | 需要 GPU，成本高 |
| 数据要求 | 需要大量高质量标注数据 |
| 时间 | 训练周期长 |
| 维护 | 知识更新需重新训练 |

---

### RAG vs 微调 对比

| 对比项 | RAG | 微调 |
|--------|-----|------|
| 知识更新 | 实时，改数据库即可 | 需重新训练 |
| 成本 | 低（向量检索） | 高（GPU训练） |
| 私有数据 | 天然适合 | 有数据泄露风险 |
| 回答风格/格式 | 不擅长 | 擅长（如统一输出格式） |
| 深度领域能力 | 依赖检索质量 | 能力内化更深 |
| 适用场景 | 知识查询、文档问答 | 特定任务、风格对齐 |

> **大多数企业级 AI 应用的主流选择：通用大模型 + RAG**，而不是自己微调一个专属模型。

---

## 三、RAG 原理详解

### 两个阶段

RAG 分为**离线入库**和**在线检索**两个阶段。

---

### 阶段一：离线入库（构建知识库）

```
原始文档（PDF/Word/文本）
        ↓
   ① 文档解析（提取纯文本）
        ↓
   ② 文本切片（Chunking）
      "把长文档切成小段，每段约 500~1000 token"
        ↓
   ③ Embedding（向量化）
      "每个文本片段 → 一个高维向量（如 1536 维浮点数组）"
        ↓
   ④ 存入向量数据库（如 PGVector）
      [chunk_id | 原文 | 向量]
```

#### 什么是 Embedding？

把文字转成数字向量，**语义相近的文字，向量距离也近**：

```
"输送机卡死"    → [0.12, 0.87, 0.34, ...]
"传送带堵塞"    → [0.11, 0.85, 0.36, ...]  ← 距离很近（语义相似）
"天气很好"      → [0.91, 0.02, 0.77, ...]  ← 距离很远（语义无关）
```

---

### 阶段二：在线检索 + 生成

```
用户提问："RGV 小车为什么停止运行？"
        ↓
   ① 问题同样做 Embedding → 查询向量
        ↓
   ② 在向量数据库中做相似度搜索（余弦相似度）
      找出最近的 Top-K 个文档片段
        ↓
   ③ 组装 Prompt：
      ┌─────────────────────────────────────┐
      │ System: 你是WCS诊断专家...           │
      │ 参考文档：                           │
      │   [片段1] RGV停止原因：急停按钮触发... │
      │   [片段2] 电机过热保护机制...         │
      │ 用户问题：RGV小车为什么停止运行？      │
      └─────────────────────────────────────┘
        ↓
   ④ LLM 基于"参考文档"生成回答
      "根据文档，RGV停止可能原因：1.急停按钮..."
```

---

### 核心算法：余弦相似度

向量数据库找"最相似片段"用的是**余弦相似度**：

```
cos(θ) = (A · B) / (|A| × |B|)

结果范围：-1 ~ 1
  → 越接近 1，语义越相似
  → 越接近 0，语义无关
```

```
查询向量 ──→ 与数据库中所有向量计算相似度
             → 按分数排序
             → 取 Top-3 或 Top-5 片段
```

---

### 切片策略的重要性

切片太大或太小都有问题：

| 切片大小 | 问题 |
|---------|------|
| 太小（< 100 token） | 语义不完整，检索噪音大 |
| 太大（> 2000 token） | 塞满上下文，无关内容多 |
| 适中（500~800 token）| 语义完整，检索精准 ✓ |

---

### RAG 完整流程图

```
用户提问
   ↓
Embedding（问题向量化）
   ↓
向量数据库（余弦相似度检索）
   ↓
Top-K 文档片段
   ↓
组装 Prompt（System + 文档片段 + 问题）
   ↓
LLM 生成回答
   ↓
返回用户（含参考来源）
```

---

### 一句话总结

> **RAG = 用问题去知识库里找相关段落，把段落塞给模型，让模型"看着答案"回答，而不是靠模型自己"背"。**
